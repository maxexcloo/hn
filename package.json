{
  "dependencies": {
    "ejs": "^6.0.1",
    "express": "^5.2.1",
    "helmet": "^8.3.0",
    "sanitize-html": "^2.17.6"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.3",
    "nodemon": "^3.1.14",
    "postcss": "^8.5.26",
    "postcss-cli": "^11.0.1",
    "prettier": "^3.9.6",
    "tailwindcss": "^4.3.3"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "license": "AGPL-3.0-only",
  "main": "index.js",
  "name": "hn",
  "scripts": {
    "build": "postcss ./public/style.css -o ./public/output.css",
    "check": "npm run build && npm run lint && npm test && npm run fmt:check",
    "dev": "npm run build && nodemon index.js",
    "fmt": "prettier --write .",
    "fmt:check": "prettier --check .",
    "lint": "node --check index.js && node --check public/comments.js && node --check public/stories.js",
    "start": "node index.js",
    "test": "node --test"
  },
  "version": "1.0.0"
}
